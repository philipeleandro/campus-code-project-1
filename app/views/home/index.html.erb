<h1>Galpões & Estoque</h1>

<h2>Galpões</h1>
<% @warehouses.each do |w| %>
  <%= w.name %>
  Código: <%= w.code %>
  Cidade: <%= w.city %>
  <%= w.area %>m²
<% end %>

<% if @warehouses.empty? %>
  <p>Não há galpões cadastrados</p>
<% end %>